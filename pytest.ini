[pytest]
minversion = 8.0
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts =
    -ra
    --strict-markers
    --tb=short
    --junitxml=reports/junit.xml
    --html=reports/report.html
    --self-contained-html
markers =
    smoke: critical launch and navigation checks
    login: login flow checks
    regression: broader APK regression checks
