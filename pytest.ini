[pytest]
testpaths = tests
pythonpath = .        ← this one line permanently fixes the error
addopts =
    --junit-xml=reports/junit.xml
    --html=reports/report.html
    --reruns=1
    --reruns-delay=3
log_cli = true
log_cli_level = INFO